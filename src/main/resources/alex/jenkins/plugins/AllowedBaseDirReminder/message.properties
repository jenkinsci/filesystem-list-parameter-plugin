TodoMessage=With version 0.0.15 allowed base dirs feature is enabled. Without additional configuration the plugin will not be allowed to list objects outside the jenkins userContent on master or nodes. The list of allowed base dirs can be configured in manage/configure page.